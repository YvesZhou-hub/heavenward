# Next actions

The playable source, optimized production export and original generated assets are delivered. The user-authorized Vercel deployment is READY at https://heavenward.vercel.app; see `DEPLOYMENT.md`. This is the resumable backlog toward the master brief's commercial V1.0 gates, not a claim of completion.

1. **Run representative human full lives.** Start from fresh ordinary drafts, without fixture HP/energy/deck grants. Include a basic-strength route, resource engine, summon/Sacrifice route and deliberate cross-Path pivot. Record seed, choices, outcome, real active play time, confusing decisions and repetitive encounters. Use the existing bot sample as reachability evidence only:20/20 wins warrant balance investigation.
2. **Tune and differentiate the five realms from evidence.** Review late-realm intent pressure, economy, offer value and node cadence. Preserve uncapped Flying Sword generation, printed combos and weighted-pivot freedom. Add authored encounters/events or cards where a specific missing decision calls for them; do not count translations/grades as new cards. Resolve the prior commercial-pool target from actual product direction rather than inventing a number.
3. **Complete presentation acceptance.** Play long legal volleys and multi-reaction turns with sound on; refine anticipation/action/impact/aftermath and distinct Armor, Dodge, Protective Qi, Bleeding, Seeds, Command, Sacrifice and Ascension feedback. Review all card illustrations in large inspection and actual-size hands; keep the generated model identity accurately marked unavailable. Do bilingual editorial/keyboard/screen-reader review of complete flows.
4. **Finish native-device checks.** Unlock the Mac and complete native Safari interaction. Repeat representative flows on declared native Chrome/Firefox/Safari versions and a lower-spec supported machine. Measure on-screen frame pacing and long sessions; headless rAF diagnostics are not proof of rendered 60 FPS.
5. **Close distribution obligations before publication.** Review current image-tool/account terms and dependency notices for the selected distribution, the current personal-test destination is already deployed and live-verified. The account is Hobby; resolve an appropriate commercial plan before commercial distribution. Future authorized updates should retain the same artifact and live-verification discipline.
6. **Evolve persistence deliberately.** Before schema2 is released, add a real schema1 migration and compatibility fixtures. Decide whether supported browsers require Web Locks or need a stronger fallback. Add server-backed/cloud saves only as an explicitly scoped product feature, with real sync/conflict handling and honest UI.

For normal local continuation: `npm ci`, `npm run dev`, then open `http://127.0.0.1:3210`. For the tested production format: `npm run build`, `node scripts/serve-static.mjs 3211`. Preserve localStorage and diagnostics when investigating a failure; do not clear user progress to make a test pass.
