# Local gameplay needs no keys, account, database, or external-service API.
# Initial navigation serves local static scripts, fonts and images.
# Image generation is a development tool and is never called by the game.
